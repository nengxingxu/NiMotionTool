<UserControl x:Class="NiMotion.View.AutoRunScript"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:vm="clr-namespace:NiMotion.ViewModel"
        mc:Ignorable="d"
        Width="900">

    <UserControl.DataContext>
        <vm:AutoRunScriptViewModel/>
    </UserControl.DataContext>
    <hc:TransitioningContentControl>
        <Grid ShowGridLines="True" HorizontalAlignment="Left" VerticalAlignment="Top">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Border Margin="5" Grid.Column="0">
                    <Button Name="BtnImportMat" Width ="100" Content="{DynamicResource ImportMat}" Padding="1" Style="{StaticResource ButtonPrimary}" hc:BorderElement.CornerRadius="5"
                     VerticalAlignment="Center" Click="Button_ImportMat_Click"/>
                </Border>
                <Border Margin="0,10, 10,10" Grid.Column="1">
                    <TextBox Width="600" Height="20" Text="{Binding MatDataPath}" IsReadOnly="True"/>
                </Border>
            </Grid>
            <Grid Grid.Row="1" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0" VerticalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"></RowDefinition>
                        <RowDefinition Height="auto"></RowDefinition>
                        <RowDefinition Height="auto"></RowDefinition>
                    </Grid.RowDefinitions>
                    <Border Margin="5" Grid.Row="0">
                        <Button Name="BtnAddScript" Width ="100" Content="{DynamicResource AddScript}" Padding="1" Style="{StaticResource ButtonPrimary}" hc:BorderElement.CornerRadius="5"
                     VerticalAlignment="Center" Click="Button_AddScript_Click"/>
                    </Border>
                    <Border Margin="5" Grid.Row="1">
                        <Button Name="BtnRemoceScript" Width ="100" Content="{DynamicResource DeleteScript}" Padding="1" Style="{StaticResource ButtonPrimary}" hc:BorderElement.CornerRadius="5"
                     VerticalAlignment="Center" Click="Button_DeleteScript_Click"/>
                    </Border>
                    <Border Margin="5" Grid.Row="2">
                        <Button Name="BtnClearScriptList" Width ="100" Content="{DynamicResource ClearScriptList}" Padding="1" Style="{StaticResource ButtonPrimary}" hc:BorderElement.CornerRadius="5"
                     VerticalAlignment="Bottom" Click="Button_ClearScriptList_Click" Margin="0,0,-0.571,0.429"/>
                    </Border>
                </Grid>
                <Border Margin="5" Grid.Column="1">
                    <ListView Name="listView" Grid.ColumnSpan="3" >
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Width="150" Header="{DynamicResource ScriptName}" DisplayMemberBinding="{Binding Name}"/>
                                <GridViewColumn Width="400" Header="{DynamicResource ScriptPath}" DisplayMemberBinding="{Binding Path}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Border>
            </Grid>
            <Grid Grid.Row="2" >
                <UniformGrid Grid.Row="1" Rows="1" Columns="7">
                    <TextBox Margin="12,10,0,10" Text="{Binding Cycles}" hc:InfoElement.Placeholder="2" Padding="5"  hc:InfoElement.Title="{DynamicResource Cycles}" Style="{StaticResource TextBoxExtend}" 
                         hc:InfoElement.Necessary="True" />
                    <TextBox Margin="12,10,0,10" Text="{Binding FPS}" hc:InfoElement.Placeholder="60" Padding="5"  hc:InfoElement.Title="{DynamicResource FPS}" Style="{StaticResource TextBoxExtend}" 
                         hc:InfoElement.Necessary="True" />
                    <TextBox Margin="12,10,0,10" Text="{Binding FlushTime}" hc:InfoElement.Placeholder="5" Padding="5"  hc:InfoElement.Title="{DynamicResource FlushTime}" Style="{StaticResource TextBoxExtend}" 
                         hc:InfoElement.Necessary="True" />
                    <TextBox Margin="12,10,0,10" Text="{Binding LED460Brightness}" hc:InfoElement.Placeholder="50" Padding="5"  hc:InfoElement.Title="{DynamicResource LED460Brightness}" Style="{StaticResource TextBoxExtend}" 
                         hc:InfoElement.Necessary="True" />
                    <TextBox Margin="12,10,0,10" Text="{Binding LED385Brightness}" hc:InfoElement.Placeholder="50" Padding="5"  hc:InfoElement.Title="{DynamicResource LED385Brightness}" Style="{StaticResource TextBoxExtend}" 
                         hc:InfoElement.Necessary="True" />
                    <ComboBox Name="CmbLED" Margin="9.5,10,-25,10" SelectedIndex="{Binding SelectedIndex}" ItemsSource="{Binding LEDList}" hc:InfoElement.Title="{DynamicResource LEDLight}" Style="{StaticResource ComboBoxExtend}"/>
      
                </UniformGrid>
            </Grid>
            <Grid Grid.Row="3" >
                <hc:StepBar Name="stepBar" Width="800" Margin="-50, 10, 10, 10" Grid.Column="5" Grid.Row="1" Grid.ColumnSpan="1" ItemsSource="{Binding StepDataList}" Dock="Top">
                    <hc:StepBar.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontSize="9" FontWeight="Bold"/>
                            </StackPanel>
                        </DataTemplate>
                    </hc:StepBar.ItemTemplate>
                </hc:StepBar>
            </Grid>
            <Grid Grid.Row="4" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Border Margin="5" Grid.Column="0">
                    <Button Name="BtnClearEnv" Width ="120" Content="{DynamicResource ClearEnvironment}" Padding="1" Style="{StaticResource ButtonPrimary}" hc:BorderElement.CornerRadius="5"
                     VerticalAlignment="Center" Click="Button_ClearPythonEnvClick"/>
                </Border>
                <Border Margin="5" Grid.Column="1">
                    <Button Name="BtnRunScript" Width ="100" Content="{DynamicResource RunScript}" Padding="1" Style="{StaticResource ButtonPrimary}" hc:BorderElement.CornerRadius="5"
                     VerticalAlignment="Center" Click="Button_RunScript_Click"/>
                </Border>
                <Border Margin="5" Grid.Column="2">
                    <Button Name="BtnStopScript" Width ="100" Content="{DynamicResource StopScript}" Padding="1" Style="{StaticResource ButtonPrimary}" hc:BorderElement.CornerRadius="5"
                     VerticalAlignment="Center" Click="Button_StopScript_Click"/>
                </Border>
            </Grid>
            <Grid Grid.Row="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Border Margin="5" Grid.Column="0">
                    <Button Name="BtnConvertToVideo" Width ="100" Content="{DynamicResource PlayDLP}" Padding="1" Style="{StaticResource ButtonPrimary}" hc:BorderElement.CornerRadius="5"
                     VerticalAlignment="Center" Click="Button_Image2Video_Click"/>
                </Border>
                <Border Margin="5" Grid.Column="1">
                    <StackPanel Orientation="Horizontal" Width="200" HorizontalAlignment="Left">
                        <ProgressBar Name="image2videoBar"  Style="{StaticResource ProgressBarFlat}" Margin="5" Width="200" Maximum="200" IsIndeterminate="False"/>
                    </StackPanel>
                </Border>
            </Grid>

         

        </Grid>
    </hc:TransitioningContentControl>
</UserControl>
